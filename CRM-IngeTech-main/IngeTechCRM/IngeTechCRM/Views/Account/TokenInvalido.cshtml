@{
    ViewData["Title"] = "Enlace Inválido";
}

<div class="flex min-h-[80vh] items-center justify-center bg-gray-50 px-4 py-12 sm:px-6 lg:px-8">
    <div class="w-full max-w-md space-y-8">
        <!-- Logo y encabezado -->
        <div class="text-center">
            <div class="mx-auto flex h-20 w-20 items-center justify-center rounded-full bg-red-100 text-red-600">
                <i class="fas fa-exclamation-triangle text-3xl"></i>
            </div>
            <h2 class="mt-6 text-3xl font-extrabold text-gray-900">Enlace Inválido</h2>
            <p class="mt-2 text-sm text-gray-600">
                El enlace para restablecer la contraseña no es válido o ha expirado
            </p>
        </div>

        <!-- Mensaje de error -->
        <div class="rounded-xl bg-white p-8 shadow-xl ring-1 ring-gray-200">
            <div class="mb-6 text-center">
                <div class="text-red-600 text-lg mb-4">
                    <i class="fas fa-times-circle mr-2"></i>
                    Error de validación
                </div>
                <p class="text-gray-700 mb-4">
                    El enlace que ha utilizado para restablecer su contraseña es inválido o ha expirado.
                    Los enlaces de restablecimiento de contraseña son válidos por 24 horas y solo pueden
                    ser utilizados una vez.
                </p>
                <p class="text-gray-700">
                    Por favor, solicite un nuevo enlace de restablecimiento de contraseña.
                </p>
            </div>

            <div class="flex justify-center space-x-4">
                <a href="@Url.Action("OlvidarContrasena", "Account")" class="rounded-lg border border-indigo-500 px-5 py-2 font-medium text-indigo-600 hover:bg-indigo-50 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                    <i class="fas fa-key mr-2"></i>
                    Solicitar Nuevo Enlace
                </a>
                <a href="@Url.Action("Login", "Account")" class="rounded-lg bg-indigo-600 px-5 py-2 font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                    <i class="fas fa-sign-in-alt mr-2"></i>
                    Ir a Iniciar Sesión
                </a>
            </div>
        </div>
    </div>
</div>

<!-- Opcional: Decoraciones visuales para el fondo -->
<div class="absolute top-0 right-0 hidden h-64 w-64 transform translate-x-1/4 -translate-y-1/4 rounded-full bg-red-500 opacity-10 blur-3xl lg:block"></div>
<div class="absolute bottom-0 left-0 hidden h-64 w-64 transform -translate-x-1/4 translate-y-1/4 rounded-full bg-red-500 opacity-10 blur-3xl lg:block"></div>