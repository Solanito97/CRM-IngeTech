@{
    ViewData["Title"] = "Dashboard de Administración";
}

<div class="container mx-auto mt-10">
    <h1 class="text-[35px] font-bold my-4 text-gray-dark">Dashboard de Administración</h1>

    <!-- Tarjetas de resumen -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-4">
        <div class="bg-brand-blue/10 border border-brand-blue/30 rounded-lg shadow-md p-4">
            <div class="flex items-center">
                <div class="mr-4">
                    <div class="text-xs font-bold text-brand-blue uppercase">Total Usuarios</div>
                    <div class="text-2xl font-bold text-gray-dark">@ViewBag.TotalUsuarios</div>
                </div>
                <i class="fas fa-users fa-2x text-gray-light"></i>
            </div>
        </div>

        <div class="bg-brand-blue/10 border border-brand-blue/30 rounded-lg shadow-md p-4">
            <div class="flex items-center">
                <div class="mr-4">
                    <div class="text-xs font-bold text-brand-blue uppercase">Total Productos</div>
                    <div class="text-2xl font-bold text-gray-dark">@ViewBag.TotalProductos</div>
                </div>
                <i class="fas fa-laptop fa-2x text-gray-light"></i>
            </div>
        </div>

        <div class="bg-gray-light/30 border border-gray-light/50 rounded-lg shadow-md p-4">
            <div class="flex items-center">
                <div class="mr-4">
                    <div class="text-xs font-bold text-gray-dark uppercase">Total Pedidos</div>
                    <div class="text-2xl font-bold text-gray-dark">@ViewBag.TotalPedidos</div>
                </div>
                <i class="fas fa-shopping-cart fa-2x text-gray-light"></i>
            </div>
        </div>

        <div class="bg-gray-light/30 border border-gray-light/50 rounded-lg shadow-md p-4">
            <div class="flex items-center">
                <div class="mr-4">
                    <div class="text-xs font-bold text-red-600 uppercase">Alertas de Inventario</div>
                    <div class="text-2xl font-bold text-gray-dark">@(ViewBag.AlertasInventario?.Count ?? 0)</div>
                </div>
                <i class="fas fa-exclamation-triangle fa-2x text-gray-light"></i>
            </div>
        </div>
    </div>

    <!-- Alertas de inventario y Últimos pedidos -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-4">
        <!-- Alertas de inventario -->
        <div class="bg-white shadow-md rounded-lg mb-4">
            <div class="p-4 flex justify-between items-center border-b">
                <h6 class="font-bold text-lg text-brand-blue">Alertas de Inventario</h6>
                <a href="@Url.Action("Alertas", "Inventario")" class="text-brand-blue hover:underline">Ver todas</a>
            </div>
            <div class="p-4">
                @if (ViewBag.AlertasInventario == null || ViewBag.AlertasInventario.Count == 0)
                {
                    <div class="text-center py-3">
                        <i class="fas fa-check-circle fa-4x text-brand-blue mb-3"></i>
                        <p class="mb-0 text-gray-dark">No hay alertas de inventario pendientes.</p>
                    </div>
                }
                else
                {
                    <div class="overflow-x-auto">
                        <table class="min-w-full divide-y divide-gray-200">
                            <thead>
                                <tr>
                                    <th class="px-4 py-2 text-left text-gray-light">Producto</th>
                                    <th class="px-4 py-2 text-left text-gray-light">Almacén</th>
                                    <th class="px-4 py-2 text-left text-gray-light">Stock Actual</th>
                                    <th class="px-4 py-2 text-left text-gray-light">Mínimo</th>
                                    <th class="px-4 py-2 text-left text-gray-light">Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var alerta in ViewBag.AlertasInventario)
                                {
                                                                        <tr class="hover:bg-gray-light/10">
                                        <td class="px-4 py-2 text-gray-dark">@alerta.Inventario.Producto.NOMBRE</td>
                                        <td class="px-4 py-2 text-gray-dark">@alerta.Inventario.Almacen.NOMBRE</td>
                                        <td class="px-4 py-2">
                                            <span class="text-red-600 font-bold">@alerta.Inventario.CANTIDAD</span>
                                        </td>
                                        <td class="px-4 py-2 text-gray-dark">@alerta.Inventario.CANTIDAD_MINIMA</td>
                                        <td class="px-4 py-2">
                                            <a href="@Url.Action("RegistrarMovimiento", "Inventario", new { ID_PRODUCTO = alerta.Inventario.ID_PRODUCTO, ID_ALMACEN = alerta.Inventario.ID_ALMACEN })" class="text-brand-blue hover:underline">
                                                <i class="fas fa-plus"></i> Agregar
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
            </div>
        </div>

        <!-- Últimos pedidos -->
        <div class="bg-white shadow-md rounded-lg mb-4">
            <div class="p-4 flex justify-between items-center border-b">
                <h6 class="font-bold text-lg text-brand-blue">Últimos Pedidos</h6>
                <a href="@Url.Action("Index", "Pedido")" class="text-brand-blue hover:underline">Ver todos</a>
            </div>
            <div class="p-4">
                @if (ViewBag.UltimosPedidos == null || ViewBag.UltimosPedidos.Count == 0)
                {
                    <div class="text-center py-3">
                        <i class="fas fa-shopping-cart fa-4x text-gray-light mb-3"></i>
                        <p class="mb-0 text-gray-dark">No hay pedidos recientes.</p>
                    </div>
                }
                else
                {
                    <div class="overflow-x-auto">
                        <table class="min-w-full divide-y divide-gray-200">
                            <thead>
                                <tr>
                                    <th class="px-4 py-2 text-left text-gray-light">#</th>
                                    <th class="px-4 py-2 text-left text-gray-light">Cliente</th>
                                    <th class="px-4 py-2 text-left text-gray-light">Fecha</th>
                                    <th class="px-4 py-2 text-left text-gray-light">Total</th>
                                    <th class="px-4 py-2 text-left text-gray-light">Estado</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var pedido in ViewBag.UltimosPedidos)
                                {
                                    <tr class="hover:bg-gray-light/10">
                                        <td class="px-4 py-2 text-gray-dark">
                                            <a href="@Url.Action("Detalles", "Pedido", new { id = pedido.ID_PEDIDO })" class="text-brand-blue hover:underline">
                                                #@pedido.ID_PEDIDO
                                            </a>
                                        </td>
                                        <td class="px-4 py-2 text-gray-dark">@pedido.Usuario.NOMBRE_COMPLETO</td>
                                        <td class="px-4 py-2 text-gray-dark">@pedido.FECHA_PEDIDO.ToString("dd/MM/yyyy HH:mm")</td>
                                        <td class="px-4 py-2 text-gray-dark">@pedido.TOTAL.ToString("C2")</td>
                                        <td class="px-4 py-2">
                                            @{
                                                var estadoClass = "";
                                                switch (pedido.ESTADO)
                                                {
                                                    case "PENDIENTE": estadoClass = "text-yellow-600"; break;
                                                    case "PROCESANDO": estadoClass = "text-blue-600"; break;
                                                    case "ENVIADO": estadoClass = "text-blue-600"; break;
                                                    case "ENTREGADO": estadoClass = "text-green-600"; break;
                                                    case "CANCELADO": estadoClass = "text-red-600"; break;
                                                }
                                            }
                                            <span class="@estadoClass font-bold">@pedido.ESTADO</span>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
            </div>
        </div>
    </div>

    <!-- Productos más vendidos y gráfico -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-4">
        <!-- Productos más vendidos -->
        <div class="bg-white shadow-md rounded-lg mb-4">
            <div class="p-4">
                <h6 class="font-bold text-lg text-brand-blue">Productos más vendidos</h6>
            </div>
            <div class="p-4">
                @if (ViewBag.ProductosMasVendidos == null || ViewBag.ProductosMasVendidos.Count == 0)
                {
                                        <div class="text-center py-3">
                        <i class="fas fa-chart-line fa-4x text-gray-light mb-3"></i>
                        <p class="mb-0 text-gray-dark">No hay datos de ventas disponibles.</p>
                    </div>
                }
                else
                {
                    <div class="overflow-x-auto">
                        <table class="min-w-full divide-y divide-gray-200">
                            <thead>
                                <tr>
                                    <th class="px-4 py-2 text-left text-gray-light">Producto</th>
                                    <th class="px-4 py-2 text-left text-gray-light">Categoría</th>
                                    <th class="px-4 py-2 text-left text-gray-light">Unidades</th>
                                    <th class="px-4 py-2 text-left text-gray-light">Total</th>
                                </tr>
                            </thead>
                            <tbody>

                                @foreach (var producto in ViewBag.ProductosMasVendidos)
                                {
                                    <tr class="hover:bg-gray-light/10">
                                        <td class="px-4 py-2 text-gray-dark">
                                            @if (producto != null && producto.Producto != null)
                                            {
                                                <a href="@Url.Action("Detalles", "Producto", new { id = producto.Producto.ID_PRODUCTO })" class="text-brand-blue hover:underline">
                                                    @producto.Producto.NOMBRE
                                                </a>
                                            }
                                            else
                                            {
                                                <span>Producto no disponible</span>
                                            }
                                        </td>
                                        <td class="px-4 py-2 text-gray-dark">
                                            @if (producto != null && producto.Producto != null && producto.Producto.Categoria != null)
                                            {
                                                @producto.Producto.Categoria.NOMBRE
                                            }
                                            else
                                            {
                                                <span>Categoría no disponible</span>
                                            }
                                        </td>
                                        <td class="px-4 py-2 text-gray-dark">
                                            @if (producto != null)
                                            {
                                                @producto.TotalVendido
                                            }
                                            else
                                            {
                                                <span>0</span>
                                            }
                                        </td>
                                        <td class="px-4 py-2 text-gray-dark">
                                            @if (producto != null && producto.Producto != null)
                                            {
                                                @((producto.TotalVendido * producto.Producto.PRECIO).ToString("C2"))
                                            }
                                            else
                                            {
                                                <span>$0.00</span>
                                            }
                                        </td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                }
            </div>
        </div>

        <!-- Gráfico de ventas -->
        <div class="bg-white shadow-md rounded-lg mb-4">
            <div class="p-4">
                <h6 class="font-bold text-lg text-brand-blue">Resumen de ventas mensuales</h6>
            </div>
            <div class="p-4">
                <div class="chart-area">
                    <canvas id="ventasChart" height="250"></canvas>
                </div>
            </div>
        </div>
    </div>

    <!-- Accesos rápidos -->
    <div class="mb-4">
        <div class="bg-white shadow-md rounded-lg">
            <div class="p-4">
                <h6 class="font-bold text-lg text-brand-blue">Acciones rápidas</h6>
            </div>
            <div class="p-4">
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
                    <div>
                        <a href="@Url.Action("Crear", "Producto")" class="block bg-brand-blue text-white text-center py-3 rounded-md hover:bg-brand-blue/90 transition">
                            <i class="fas fa-plus-circle mr-2"></i> Nuevo Producto
                        </a>
                    </div>
                    <div>
                        <a href="@Url.Action("RegistrarMovimiento", "Inventario")" class="block bg-gray-dark text-white text-center py-3 rounded-md hover:bg-gray-dark/90 transition">
                            <i class="fas fa-boxes mr-2"></i> Movimiento de Inventario
                        </a>
                    </div>
                    <div>
                        <a href="@Url.Action("Crear", "Comunicado")" class="block bg-brand-blue text-white text-center py-3 rounded-md hover:bg-brand-blue/90 transition">
                            <i class="fas fa-envelope mr-2"></i> Nuevo Comunicado
                        </a>
                    </div>
                    <div>
                        <a href="@Url.Action("EnviarMensaje", "WhatsApp")" class="block bg-gray-dark text-white text-center py-3 rounded-md hover:bg-gray-dark/90 transition">
                            <i class="fab fa-whatsapp mr-2"></i> Enviar WhatsApp
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
                $(document).ready(function () {
            // Obtener datos del servidor
            var meses = @Html.Raw(Json.Serialize(ViewBag.EtiquetasMeses));
            var ventas = @Html.Raw(Json.Serialize(ViewBag.DatosVentas));

            var ctx = document.getElementById('ventasChart').getContext('2d');
            var ventasChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: meses,
                    datasets: [{
                        label: 'Ventas (₡)',
                        data: ventas,
                        backgroundColor: 'rgba(0, 93, 164, 0.2)', // brand-blue with alpha
                        borderColor: 'rgba(0, 93, 164, 1)', // brand-blue
                        borderWidth: 2,
                        pointBackgroundColor: 'rgba(0, 93, 164, 1)', // brand-blue
                        pointBorderColor: '#fff',
                        pointHoverBackgroundColor: '#fff',
                        pointHoverBorderColor: 'rgba(0, 93, 164, 1)', // brand-blue
                        tension: 0.4
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: {
                        y: {
                            beginAtZero: true,
                            ticks: {
                                callback: function(value) {
                                    return '₡' + value.toLocaleString();
                                }
                            }
                        }
                    },
                    plugins: {
                        tooltip: {
                            callbacks: {
                                label: function(context) {
                                    return 'Ventas: ₡' + context.parsed.y.toLocaleString();
                                }
                            }
                        }
                    }
                }
            });
        });
    </script>
}