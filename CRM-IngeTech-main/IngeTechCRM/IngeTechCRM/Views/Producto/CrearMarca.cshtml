
@model IngeTechCRM.Models.Marca
@{
    ViewData["Title"] = "Crear Marca";
}

<div class="container mx-auto px-4 py-6">
    <div class="mb-6 flex items-center justify-between">
        <h1 class="text-2xl font-bold text-gray-800 md:text-3xl">Crear Nueva Marca</h1>
        <a href="@Url.Action("Marcas")" class="flex items-center rounded-lg bg-gray-100 px-4 py-2 font-medium text-gray-700 transition hover:bg-gray-200">
            <i class="fas fa-arrow-left mr-2"></i> Volver
        </a>
    </div>

    <div class="overflow-hidden rounded-xl bg-white shadow-lg">
        <div class="bg-gradient-to-r from-indigo-600 to-blue-500 px-4 py-3 text-white">
            <h2 class="flex items-center font-bold">
                <i class="fas fa-plus-circle mr-2"></i> Formulario de Marca
            </h2>
        </div>
        <div class="p-6">
            @if (TempData["Message"] != null)
            {
                <div class="mb-4 rounded-lg bg-red-100 p-4 text-red-700">
                    <div class="flex">
                        <div class="flex-shrink-0">
                            <i class="fas fa-exclamation-circle"></i>
                        </div>
                        <div class="ml-3">
                            <p class="text-sm">@TempData["Message"]</p>
                        </div>
                    </div>
                </div>
            }

            <form asp-action="CrearMarca" method="post">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="mb-4 rounded-lg bg-red-100 p-4 text-red-700"></div>

                <div class="space-y-6">
                    <div>
                        <label asp-for="NOMBRE" class="mb-1 block text-sm font-medium text-gray-700">Nombre</label>
                        <input asp-for="NOMBRE"
                               type="text"
                               class="block w-full rounded-lg border border-gray-300 bg-white px-3 py-2 shadow-sm focus:border-indigo-500 focus:outline-none focus:ring-indigo-500"
                               placeholder="Nombre de la marca">
                        <span asp-validation-for="NOMBRE" class="mt-1 text-sm text-red-600"></span>
                    </div>

                    <div>
                        <label asp-for="DESCRIPCION" class="mb-1 block text-sm font-medium text-gray-700">Descripción</label>
                        <textarea asp-for="DESCRIPCION"
                                  rows="3"
                                  class="block w-full rounded-lg border border-gray-300 bg-white px-3 py-2 shadow-sm focus:border-indigo-500 focus:outline-none focus:ring-indigo-500"
                                  placeholder="Descripción opcional de la marca"></textarea>
                        <span asp-validation-for="DESCRIPCION" class="mt-1 text-sm text-red-600"></span>
                    </div>
                </div>

                <div class="mt-8">
                    <div class="rounded-lg bg-blue-50 p-4 text-blue-700">
                        <div class="flex">
                            <div class="flex-shrink-0">
                                <i class="fas fa-info-circle"></i>
                            </div>
                            <div class="ml-3">
                                <h3 class="text-sm font-medium">Información</h3>
                                <div class="mt-2 text-sm">
                                    <p>Las marcas permiten categorizar productos según su fabricante.</p>
                                    <ul class="ml-4 mt-1 list-disc">
                                        <li>Asegúrese de usar el nombre oficial de la marca</li>
                                        <li>La descripción puede incluir información general sobre la marca</li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="mt-6 flex justify-end space-x-3">
                    <a href="@Url.Action("Marcas")" class="rounded-lg border border-gray-300 bg-white px-4 py-2 font-medium text-gray-700 shadow-sm hover:bg-gray-50">
                        Cancelar
                    </a>
                    <button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2 font-medium text-white shadow-sm hover:bg-indigo-700">
                        Guardar
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
